// PlacedFlower — a single draggable flower rendered on the BouquetCanvas
// Wraps framer-motion drag + scale animations
// Props: flower (PlacedFlowerItem), onRemove, onSelect
export default function PlacedFlower() {
  return null;
}
