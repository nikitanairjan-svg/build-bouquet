// FlowerCard — individual flower tile inside FlowerPanel
// Shows image, name, colour swatch, and an add-to-bouquet button
// Props: flower (FlowerItem), onAdd callback
export default function FlowerCard() {
  return null;
}
