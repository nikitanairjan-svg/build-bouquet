// FlowerPanel — left/bottom sidebar listing available flowers to pick
// Grouped by type (roses, wildflowers, foliage, etc.)
// Clicking a flower adds it to the bouquet via bouquetStore
export default function FlowerPanel() {
  return null;
}
